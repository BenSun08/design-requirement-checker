# 原型验证与范围审查

[English source](../docs/prototype-verification.md)

本文件为同名英文文档的对应中文版本；字段、状态、路径与命令保留原技术标识。

日期：2026-09-10。环境：macOS 主机、Codex 内置浏览器、本地静态服务器。这是 UI／夹具验证，不是真实 DOCX 正确性或 Windows 资格验证。

## 历史浏览器观察记录（证据不变）

| 检查项 | 观察结果 |
|---|---|
| 空导入／选择示例 | 显示文件名、明确运行操作、可见模拟声明 |
| 加载 | 模拟进度；生成时禁用选择／运行／管理 |
| 完成汇总 | 总数 11／已配置 8／未配置 2／已划线 1／独立描述差异 1 |
| 筛选 | 已配置 8、未配置 2；删除线详情；仅异常 4；总数仍 11 |
| 搜索 | `2门` 得到 2 项；不存在的词得到 0 项，并清空详情 |
| 比较 | DR-006 为 CONFIGURED + EXACT，同时独立显示描述差异与 2s → 3s |
| 证据 | NORMALIZED／ALIAS 解释；未配置无虚构位置；删除线文本保留 |
| 基准 CRUD | 新增临时项、改名、禁用，基准变更使结果失效；修改后的删除对话框可确认移除；刷新恢复夹具 |
| 键盘 | Arrow Down 改变选中证据；最终修复后筛选焦点保留 |
| 不支持文件 | README.md 被拒绝并提示 `.docx`；运行仍禁用 |
| 布局 | 查看桌面分栏；1024 宽时 DOM scrollWidth=1024，390 宽时 scrollWidth=390；截图仅内联查看，未保存 |

app.js 和 mock-data.js 的 Node 语法检查通过。结构／内容检查记录在原任务最终输出。无需包构建。

## 审查中的修复

浏览器原生确认框阻塞自动化后，改用页面内删除对话框。为编辑器增加可访问名称；筛选、启用／禁用、清空搜索重建控件后恢复焦点。别名命中不再声称描述相同。新增／实质修改但没有夹具的项目，明确说明未配置只是演示，不是文档检测结果。功能词示例当时标为仅夹具使用，并列出 Q9；用户现在已批准独立 detectionPhrase，但本次文档更新没有改写历史夹具。

独立代理审查了代码／规格并确认其列出的修复已完成；它不是独立截图／无障碍审计。Impeccable 可选上下文引擎不可用，当时直接读取设计指导。

## 限制与后续

- 未解析真实 DOCX，未测量匹配精确率／召回率或格式准确率。
- 有效 DOCX 选择和 OS 拖放处理已编写，但没有独立实际操作有效文件／系统拖放；示例导入及错误扩展名经过操作。
- 浏览器 URL 策略阻止 `file://`，因此未通过工具测试双击打开。使用相对 classic scripts/styles，无 fetch/modules；需由用户在 Edge/Chrome 检查。localhost 路径已操作。
- 没有 Windows VM、安装程序、Windows 中文 IME、屏幕阅读器或打包测试。取消／Escape／重复验证已实现并代码审查，但并非所有分支经过浏览器操作。
- 没有执行真实技术实验；提议见 technical-spikes.md。

## 过度设计审查

保留四层概念边界、证据范围／位置、基准／核查来源及明确未决规则，用于追溯，不意味着服务层或保存历史的产品。

推迟 AI、语义／模糊引擎、完整渲染器、多进程核心、更新器、导出、Word 集成、DOCX 改写、团队同步、插件框架和详细生产排期。由于已选 Python/PySide6 Qt Widgets 且无 sidecar，旧条件性 IPC 实验已撤销。编辑仍属未来范围，结构保留实验仅供参考。

用户已批准可空 status 配合 resolution，以及明确匹配短语。嵌套／合并表格仍需提取验证；这些概念应与实际支持的文档复杂度相称。

## 用户确认后的附记

生产采用 Python + PySide6 / Qt Widgets，面向 Windows 10/11 x64 无管理员／安装权限使用者。产品确认项 1–7 已记入 product-spec.md；本附记是文档审查，不是新的执行记录。

本次没有修改或重新运行浏览器原型，上述结果仍为历史事实。它还没有展示独立 detectionPhrase 字段、UNRESOLVED 结果／计数／筛选、多处／冲突证据、部分删除线规则、范围提示或持久化基准。DR-006 夹具使用宽泛 `2门控制` 解释；生产需使用工程师维护的足够具体短语。会话级管理和本地浏览器启动不能证明 Windows／无管理员部署可行。

后续证据来自已规划的 S1/S2/S3/S6 和 Qt UI／Windows 测试。接受技术选择不意味着夹具准确、无管理员打包成功、当前版本兼容或生产质量已获证明。

## 已确认的 Python 3.8 与完全离线环境

用户说明：公司电脑目前安装 **Python 3.8**，能否升级未知。这是现有环境事实，不表示已经选择应用／构建运行时版本。保留该安装及其 PATH／文件关联，不作修改。目标仍为 **Windows 10 和 Windows 11 x64**，使用者**没有管理员／安装权限**。**首次分发、解压／安装、首次启动和正常核查必须完全离线。**

计划的自包含包须携带经过验证的 Python／Qt／原生依赖，不调用电脑中的 `python`，不要求目标机执行 `pip install`、在线激活或下载。S3 必须断网、无缓存前置依赖，分别验证与现有 Python 3.8 共存且不改动它，以及未安装 Python 的干净电脑。目前没有分发包通过这些检查。

开发／构建环境是否可用仍是独立未决项：确认能否在不改变公司安装的条件下，使用获准的隔离／较新解释器及 Windows 构建机。不能认为 Python 3.8 的 `venv` 会升级解释器。如果唯一允许的构建／运行时就是 3.8，应先评估准确兼容依赖组合及维护影响，不能悄悄锁定旧包或改变技术栈。完全离线交付不代表已经确认构建机本身能否联网；需要单独记录，并在必要时准备离线构建依赖。

最新环境确认和中文翻译都只是文档更新，没有为历史原型记录增加 Python 3.8 兼容、离线部署或首次启动的测试结果。
