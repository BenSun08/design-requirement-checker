# 产品交付路线图

[English source](../docs/delivery-roadmap.md)

本文件为同名英文文档的对应中文版本；字段、状态、路径与命令保留原技术标识。

状态：用户确认后更新。生产为 **Python + PySide6 / Qt Widgets**；部署为 **Windows 10/11 x64，无管理员／安装权限**。路线图仍按用户可见成果描述，详细任务见 implementation-plan.md。每周约八小时，本次文档更新不执行生产代码或实验。

| 里程碑 | 用户可见成果 | 验收条件 | 剩余验证／门槛 |
|---|---|---|---|
| 0 定义、原型与决策 | 可审查的模拟原型，以及已确认的规则／技术栈 | 保留历史原型；记录独立匹配短语、三状态＋待核查、冲突／部分删除线、一套基准和范围 | 决策门槛已完成；原型未展示所有新规则 |
| 1 真实 DOCX 导入 | 打开支持的本地文件，查看文字／格式／来源 | 正文／表格／单元格段落与 run 映射正确，不跨段／单元格连接；未知／不支持范围明确；原件不变 | S1/S2、样式继承、嵌套／合并表格、准确库版本 |
| 2 确定性核查 | 按配置短语／别名给出可解释结果 | 已确认分类优先级、所有相关证据、保守规范化、必填 detectionPhrase；不猜测功能身份 | S6 关联要求范围、转换白名单、语料歧义 |
| 3 端到端审查 | 导入 → 运行 → 异常 → 预期／实际／上下文 | 三状态加待核查计数正确；范围受限提示；全部出现可查看；错误／取消区别于未配置；UI 响应 | Windows Qt 可用性、比较限制、性能和取消 |
| 4 本地基准管理 | 维护一套基准，重启后保留 | 新增／编辑／禁用／删除、匹配短语／别名；启用集适用当前订单；免提权保存／恢复和结果失效 | 存储格式／位置、写入失败恢复；无模板或同步 |
| 5 历史验证 | 工程师判断试点可靠性 | 标注语料；精确率／召回率、误报／漏报、删除线准确率、待核查／不支持比例、审查时间变化及分母 | 代表性样本、试点前约定发布阈值。**2026-09-23：**评测契约／评测器／指标／报告渲染基础设施已实现并通过自测；**BLOCKED**——未获得真实／脱敏、独立标注语料，所有指标 NOT MEASURED（分母 0），阈值仅为提案（PENDING OWNER APPROVAL），未获约定。 |
| 6 Windows 分发与试点 | 普通用户可运行和更新 | 两类 x64 Windows；无管理员权限和额外开发运行时；首次分发／安装／启动／核查完全离线；替换保留基准；便携或允许的用户级形式 | S3 提前验证、与 Python 3.8 共存、具体 OS 构建及 IT 策略；首次完全离线分发／安装为硬性要求 |

S3 刻意提前，即使生产试点在里程碑 6。用户不能提权，因此不能默认采用管理员安装程序。优先验证便携目录；只有策略允许时才保留用户级安装选项。如果策略阻止所有用户级程序执行，应解决 IT 依赖，不能绕过。

里程碑 2 消费基准数据，里程碑 4 才加入完整编辑／持久化 UX。预期／实际比较属于 MVP；增强 diff、模糊匹配、人工确认、导出和预览改进等待试点反馈。AI、编辑、Word 集成、多基准和团队同步需要单独决定。

实施仍需明确执行授权和相关验证证据。验证失败先在所选技术栈内作有限复核；更换技术栈需要用户重新决定。进度目标根据实验修订，不能根据模拟原型已完成来推算。

## 已确认的 Python 3.8 与完全离线环境

用户说明：公司电脑目前安装 **Python 3.8**，能否升级未知。这是现有环境事实，不表示已经选择应用／构建运行时版本。保留该安装及其 PATH／文件关联，不作修改。目标仍为 **Windows 10 和 Windows 11 x64**，使用者**没有管理员／安装权限**。**首次分发、解压／安装、首次启动和正常核查必须完全离线。**

计划的自包含包须携带经过验证的 Python／Qt／原生依赖，不调用电脑中的 `python`，不要求目标机执行 `pip install`、在线激活或下载。S3 必须断网、无缓存前置依赖，分别验证与现有 Python 3.8 共存且不改动它，以及未安装 Python 的干净电脑。目前没有分发包通过这些检查。

开发／构建环境是否可用仍是独立未决项：确认能否在不改变公司安装的条件下，使用获准的隔离／较新解释器及 Windows 构建机。不能认为 Python 3.8 的 `venv` 会升级解释器。如果唯一允许的构建／运行时就是 3.8，应先评估准确兼容依赖组合及维护影响，不能悄悄锁定旧包或改变技术栈。完全离线交付不代表已经确认构建机本身能否联网；需要单独记录，并在必要时准备离线构建依赖。
